/* import React from 'react'; */

/* Skapade en separat komponent till Albums, men får den inte att fungera:
 */

/*const Albums = (title, titleClick) => {
  return (
    <a href={titleClick} target='_blank'>
      <h2>{title}</h2>
    </a>
  );
};

export default Albums;
 */
